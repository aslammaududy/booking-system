type Availability{
    id: Int!
    weekday: Int!
    start_time: String!
    end_time: String!
}

type AvailabiltyMutation {
    updateAvailability(id: Int!, start_time: String!, end_time: String!): Availability @update
}

